# 2026-08-13 工作日志

## 需求规格说明书图表质量修正
- 问题：用户反馈图 4-1 角色权限架构图失真，且所有架构/流程图存在颜色过深、字体不清、底部深色条带文字不显示等问题。
- 根因：SVG 中全局 `text { fill:#334155 }` 的 CSS 优先级高于 `fill="#FFFFFF"` 属性，导致深色条带内的白色文字被覆盖为深色，与背景同色而不可见。
- 修正内容：
  1. `box / pill / decision` 的文字改用 `style="fill:..."` 内联样式，确保白色文字在深色背景上可见。
  2. 全局 `.node-sub` 颜色从 slate500 加深到 slate600，提升副标题可读性。
  3. 重写 `d_rbac`：增大画布、改为卡片式布局、移除突兀的 "…" 标签。
  4. 重写 `d_er`：采用三列错行布局，关系线走独立通道并加白色背景标签，避免线与实体文字重叠。
  5. 重写 `d_process`：加宽中台、分散左右连线连接点，避免汇聚成一团。
  6. 统一 `d_business` 底层条带颜色为蓝色系，保持视觉一致。
  7. 12 张业务流程图随样式更新自动改善。
- 结果：22 张图表全部重新生成并渲染为高清 PNG，重新生成 `docs/需求规格说明书.docx`（20.14 MB，145 张图，0 缺失）。
